test:
    name: basic-contract-benchmark
    description: test benchmark
    workers:
      number: 10
    rounds:
      - label: readAsset
        description: Read did benchmark
        txDuration: 30
        rateControl:
          type: fixed-load
          opts:
            transactionLoad: 20
        workload:
          module: workload/readAsset.js
          arguments:
            assets: 100
            contractId: basic